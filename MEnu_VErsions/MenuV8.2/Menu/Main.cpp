#include "ChangeTheJ.h"
#include "Fibonum.h"

#include <iostream>
using namespace std;
int main() {
  int input;
  cin>>input;
  switch case(input)
  case  (1) {
    int x, y, z, d;
    ChangeTheJ(x, y, z, d);
    break;
  } 
 case(2) {
    int x, y, z, d, a;
    Fibonum(d);
    break;
  }
  return 0;
}
